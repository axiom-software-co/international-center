name: international-center-dev
runtime: go
description: International Center Development Infrastructure
template:
  config:
    environment:
      description: The deployment environment
      default: development